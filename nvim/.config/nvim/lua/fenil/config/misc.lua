require('vim._core.ui2').enable()

-- :help python-provider, we don't care about python, perl remote plugins etc
vim.g.loaded_python3_provider=0
vim.g.loaded_perl_provider=0
vim.g.loaded_ruby_provider=0
